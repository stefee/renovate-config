{
  "description": "Disables automerge for testing frameworks. Upgrades to testing frameworks need to be validated manually to avoid false positive results.",
  "packageRules": [
    {
      "matchPackageNames": ["nock", "jsdom"],
      "matchPackagePatterns": ["^cypress", "^jest", "^@pollyjs"]
      "automerge": false,
      "groupName": "test dependencies",
      "minor": {
        "automerge": false,
        "groupName": "test dependencies"
      },
      "pin": {
        "automerge": false,
        "groupName": "test dependencies"
      },
      "digest": {
        "automerge": false,
        "groupName": "test dependencies"
      }
    }
  ]
}
